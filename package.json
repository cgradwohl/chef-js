{
  "name": "chef",
  "version": "1.0.0",
  "description": "recipes",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx tsc",
    "watch": "npx tsc --outDir build/ --watch"
  },
  "author": "gwohl",
  "license": "ISC",
  "devDependencies": {
    "prettier": "2.6.2",
    "typescript": "^4.6.4"
  }
}
